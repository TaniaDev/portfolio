import { createStitches } from '@stitches/react'

export const {
  config,
  styled,
  css,
  globalCss,
  keyframes,
  theme,
  createTheme,
  getCssText
} = createStitches({
  theme: {
    colors: {
      portfolio: '#8257e6',
    }
  }
})